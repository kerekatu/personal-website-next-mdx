---
title: 'My Third Post'
publishedAt: '10-10-2020'
categories: ['React', 'Next Js']
excerpt: "This Post Contains All Knowledge Known To Man except for everything else that you would learn if you did study for more than one hour a day... actually this blog post doesn't include a lot of knowledge tbh. Anyway, enjoy."
coverImg:
  {
    avif: '/static/posts/first-post/test.avif',
    jpg: 'static/posts/first-post/test.jpg',
  }
---

### First Title

This is a test blog post for testing various html elements and react components as well.

Lorem ipsum dolor sit amet consectetur adipisicing elit. Nisi a ducimus accusamus repellat libero, reprehenderit hic facere laudantium rerum ut **Strong Bold Text** commodi debitis earum . Cumque corrupti porro nam vitae adipisci.
[Here is a link](https://github.com/adam-p/markdown-here/wiki/Markdown-Cheatsheet)

```jsx
const works = true

if (works) {
  console.log(tree)
}
```

#### Second Title

Let's take a look at **Example.js:**

```jsx
<SectionRow>
  {categories.map((category) => (
    <Button
      variant={activeCategory === category ? 'quaternary active' : 'quaternary'}
      label={category}
      key={category}
      onClick={() => {
        filterPostsWithCategory(category)
      }}
    />
  ))}
  {activeCategory && (
    <Button
      variant="quaternary active"
      label="Reset"
      icon={faTimes}
      onClick={() => {
        setActiveCategory(null)
        setFilteredPosts(postsData)
      }}
    ></Button>
  )}
</SectionRow>
```
